# Blog Image Assets

Use these images in the AWS Builder Center article.

| Image | Suggested caption | Why include it |
| --- | --- | --- |
| `docs/screenshots/06-architecture-diagram.png` | MPP Pulse AWS architecture | Shows the full EventBridge, Lambda, DynamoDB, Bedrock, and S3 flow. |
| `docs/screenshots/05-lambda-function.png` | Lambda function deployed in AWS | Shows the core always-on agent running as one AWS Lambda function. |
| `docs/screenshots/03-html-report.png` | Generated MPP Pulse daily report | Shows the output waiting after the agent runs. |
| `docs/screenshots/04-autonomous-invocation.png` | Autonomous scheduled invocation evidence | Shows the agent fired without a manual button click. |
| `docs/screenshots/02a-eventbridge-enabled.png` | EventBridge Scheduler enabled | Shows the daily schedule is enabled for the always-on behavior. |

Recommended article order:

1. Architecture diagram near the top.
2. Lambda screenshot in "Vision and What the Agent Does" or "How I Built It".
3. HTML report screenshot after describing the report.
4. Autonomous invocation screenshot in the architecture section.
5. EventBridge enabled screenshot if Builder Center allows another supporting image.
